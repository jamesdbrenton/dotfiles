# Load the system-wide profile
[[ -f /etc/profile ]] && . /etc/profile

# If running bash, read the user's .bashrc
[[ -n "$BASH_VERSION" && -f ~/.bashrc ]] && . ~/.bashrc

export PATH="$HOME/.local/bin:$PATH"
export EDITOR="$HOME/nvim-linux-arm64/bin/nvim"
